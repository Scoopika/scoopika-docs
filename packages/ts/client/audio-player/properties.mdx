You can check these 2 properties to know what the player state is:

<ParamField path="paused" type="boolean">
    Set to `true` if the player is paused
</ParamField>

<ParamField path="started" type="boolean">
    Set to `true` if the player has started playing audio
</ParamField>