---
title: 'Quickstart'
description: 'Start building AI-powered applications in under 5 minutes'
---

<Steps>
  <Step title="Create your account">
    Visit the [Scoopika platform](https://scoopika.com/app) and sign up using your Google or Github account.
  </Step>
  <Step title="Create your first agent">
    Creating a new agent is a breeze. Simply provide a name, optional description, avatar, and a system prompt to guide its behavior. You can even generate agent avatars using AI directly from the platform. [learn more about system prompts and variables]().

    <Tip>Choose the LLM that will drive the agent from a selection of available hosts and models.</Tip>

    <Frame>
      <img src="images/new-agent.gif" />
    </Frame>

  </Step>
  <Step title="Generate token">
    Generate an access token to be able to use Scoopika in your applications

    <Warning>Remember to keep your tokens secure and never expose them to the public or client-side.</Warning>

    <Frame>
      <img src="images/generate-token.gif" />
    </Frame>

  </Step>
  <Step title="Run the agent">
    Start running the agent in your application. First, install the Scoopika package:

```bash
npm i @scoopika/scoopika
```

    Then initialize a Scoopika instance:

```typescript
import { Scoopika, Agent } from "@scoopika/scoopika";

const scoopika = new Scoopika({
  token: "YOUR_SCOOPIKA_TOKEN",
  engines: {
    openai: "YOUR_OPENAI_KEY"
  }
});
```

    <Info>
      Notice that We added openai to our engines because We're using OpenAI's gpt-3.5 in this example (default option). [learn more about engines]()
    </Info>

    Now utilize the agent and make the first run:

```typescript
const agent = new Agent("AGENT_ID", scoopika);

const run = await agent.run({
  inputs: {
    message: "Hello"
  }
});

console.log(run);
```

    <Info>Agent ID can be found under the agent name, just click it to copy!</Info>

  </Step>

  <Step
    title="Next steps"
  >
    We recommend you check these resources next:

    * [Understand Scoopika](/understand-scoopika).
    * [How to work with agents](/agents).
    * [Scoopika for the web](/scoopika-for-the-web)

    Key features:
    * [History stores](/history-stores): To have persistent chat history.
    * [Tools](/history-stores): Connect your agents to custom functions.
  </Step>

</Steps>